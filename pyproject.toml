[build-system]
requires = ["setuptools>=61.0.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "scqat"
version = "0.24.0"  # MUST equal the release tag (tag vX.Y.Z <-> version X.Y.Z); bump BEFORE tagging.
                   # Dependents pin real floors (scqo: "scqat>=0.1.5") via importlib.metadata, so a tag
                   # whose tree still says the old version breaks their installs. (Tags v0.1.0-v0.1.4
                   # all shipped metadata 0.1.0 -- floors only work from v0.1.5 on.)
description = "Superconducting qubit analysis tool for experimental data pipelines."
authors = [
    {name = "Li-Chieh Hsiao", email = "shiau109@gmail.com"}
]
readme = "README.md"
license = { text = "BSD-3-Clause" }
# The combo window: SCQO, scqo-qm and scqo-qblox all pin this. An unbounded floor let
# `uv run` build scqat/.venv on Python 3.14 while every other environment in the stack was
# 3.12.13 or 3.11.15 - the analysis half tested on a Python the rest cannot install on.
# CI covers 3.12 + 3.10; 3.9 was never in the matrix. See SCQO/ENVIRONMENTS.md.
requires-python = ">=3.10,<3.13"
dependencies = [
    "numpy>=1.24.0",
    "scipy>=1.13.0",
    "matplotlib>=3.9",
    "xarray>=2024.6.0",
    "lmfit>=1.2.0",
    "scikit-learn>=1.3.0",
    # "juliacall>=0.9.0"  # Uncomment later if you drop in Julia for heavy MIST simulations
]

[project.optional-dependencies]
dev = [
    "pytest>=7.0.0",
    "jupyter>=1.0.0",
    "ipykernel>=6.0.0"
]

[tool.setuptools.packages.find]
where = ["."]
include = ["scqat*"]